export const GRAPHICS_WIDTH = 320;
export const GRAPHICS_HEIGHT = 240;

/** Black padding (in device pixels) kept inside the visible canvas's edges so bilinear
 * upscaling doesn't get clipped at the edges. */
export const CANVAS_EDGE_PADDING = 1;
